# GitHub Models API Token (for LLM features)
VITE_GITHUB_TOKEN=

# Optional STM bridge settings for /dashboard
# Leave enabled for automatic fallback to local heuristics if STM API is unavailable.
VITE_ENABLE_STM=true
# Default local proxy target is /stm -> http://localhost:8000 (see vite.config.ts)
VITE_STM_API_URL=/stm
